const SearchBox = () => {
  return (
    <div>
      <input
        className="border-2 px-2 rounded-sm bg-slate-100"
        placeholder="Search a hero..."
      ></input>
    </div>
  );
};

export default SearchBox;
